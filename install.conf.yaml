- clean: ['~']

- link:
    ~/.profile: profile
    ~/.bashrc: bashrc
    ~/.vimrc: vimrc
    ~/.zshrc: zshrc
- shell:
    - apt-get install screen tmux git hg svn bzr fossil acpi
    - git clone https://github.com/nojhan/liquidprompt.git /opt/liquidprompt



